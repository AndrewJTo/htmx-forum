<body>
  <h1>Category: {{ .Name }}</h1>
  <p>{{ .Description }}</p>
    <table>
      <tr>
        <th>Name</th>
        <th>Options</th>
      </tr>
      {{ range .threads}}
        <tr>
          <td>{{ .Name }}</td>
          <td><button hx-get="/thread/{{.Id}}" hx-target="body" hx-push-url="true">View</button></td> 
        </tr>
      {{ end }}
    </table>
</body>